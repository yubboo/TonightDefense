/**
 * @architecture TonightDefense V1.0
 * @owner shared
 * @module root
 * @migratedFrom shared/GameEnums.ts
 */
export enum GameMode {
    Solo = 'solo',
    Multiplayer = 'multiplayer',
}

export enum PartyMemberType {
    Human = 'human',
    Companion = 'companion',
}

export enum ProgressPhase {
    Recruit = 'recruit',
    Build = 'build',
}
